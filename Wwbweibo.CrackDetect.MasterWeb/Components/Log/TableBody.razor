
@foreach (var log in Logs)
{
    index = index + 1;
    <TableRow Index="index"  LogMessage="@log.LogMessage" ServiceType="@log.OriginServiceType" LogLevel="@log.LogLevel" LogTime="@log.LogTime"/>
}

@code{
    private int index = 0;
    [Parameter]
    public Wwbweibo.CrackDetect.Models.LogModel[] Logs { get; set; }
    [Parameter]
    public EventCallback<Wwbweibo.CrackDetect.Models.LogModel[]> LogsChanged { get; set; }
}